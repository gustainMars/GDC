<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gustainmars.gdcapplication">

    <application
        android:name=".GdcApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GdcApplication">
        <activity
            android:name=".GdcActivity"
            android:exported="true"
            android:label="@string/title_activity_gdc"
            android:theme="@style/Theme.GdcApplication.NoActionBar" />
    </application>

</manifest>